name: Trigger for MR

on:
  pull_request:
    branches: [ main ]
    types: [closed]

jobs:
  trigger:
    if: ${{ github.event.pull_request.merged }}
    runs-on: ubuntu-latest
    steps:
    - run: java -jar jenkins-cli.jar -s http://34.229.75.164:8080/ -webSocket build Pipeline-1

